import { ReactElement } from 'react';
import { InputProps } from './Input.types';
export default function Input({ autocomplete, label, name, type, onChange, wrapperProps, ...options }: InputProps): ReactElement;
