import { ReactElement } from 'react';
import { ALInputProps } from './ALInput.types';
export default function ALInput({ autoComplete, error, inputProps, name, type, onChange, ...wrapperProps }: ALInputProps): ReactElement;
