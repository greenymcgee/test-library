import { ReactElement } from 'react';
import { ALInputGroupProps } from './ALInputGroup.types';
export default function ALInputGroup({ error, label, name, type, inputProps, ...options }: ALInputGroupProps): ReactElement;
